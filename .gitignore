# Ignore everything by default
*

# But don't ignore these files
!parser.py
!README.md
!requirements.txt
!.gitignore

# Don't ignore directories (so we can traverse them)
!*/

# But ignore everything in subdirectories
*/*

# Exception: if you want to include specific files in subdirectories, add them here
# Example: !subdirectory/important_file.py
